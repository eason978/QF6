1. learning key: 把learning到的key存到eMMC。AP目前只存最後learning的key
2. send learned key: 從eMMC裡，把最後learning到的key讀出來送出去
3. send A/V database key: 先選brand，再選device。
   device的button按下去後就會自動送出該device的power key (AP目前是寫死只送power key)。
   Nicole旁邊位置的大台電視，用SONY的151和861兩種device的power key都能控制
4. received key: 只收遵宇專用搖控器TLT-SRC-3200